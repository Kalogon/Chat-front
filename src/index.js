import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css';
// import socketio from 'socket.io-client'

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
// const socket_client = socketio.connect("localhost:8080")